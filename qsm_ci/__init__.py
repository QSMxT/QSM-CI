"""qsm-ci — command-line companion for the QSM-CI reconstruction challenge.

Scaffold a submission (`qsm-ci new`), test it locally against the public dev phantom
(`qsm-ci test`), and open a pull request (`qsm-ci submit`). The scorer is the exact same
code the challenge CI runs (`qsm_ci.qsm_eval`), so local numbers match the leaderboard.
"""

__version__ = "0.1.0"
