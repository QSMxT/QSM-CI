# Build-time dependencies, fetched per BUILD.md — not committed.
nifti/
sti/

# mcc output — the binary is baked into the image, not stored in git.
recon
run_recon.sh
buildresult.json
includedSupportPackages.txt
mccExcludedFiles.log
readme.txt
requiredMCRProducts.txt
