name: UK Biobank QSM pipeline
slug: ukb-qsm
algorithm: ukb-qsm
source: ukb
stage: end-to-end
language: MATLAB
family: iterative
learning: none
engine: UK Biobank + STI Suite
description: The susceptibility pipeline UK Biobank runs on its swMRI data, as published by the
  Oxford group — phase-variance masking, Laplacian unwrapping, T2*-weighted echo combination,
  V-SHARP background removal (12 mm kernel) and iLSQR dipole inversion. Submitted so the
  reference pipeline behind the UK Biobank susceptibility IDPs can be placed on the same
  benchmark as everything else. Coil combination (MCPC-3D-S) sits upstream of this stage and is
  not reproduced, since QSM-CI supplies already-combined phase.
authors:
- name: Chaoyue Wang
- name: Benjamin C. Tendler
- name: Karla L. Miller
doi: 10.1038/s41593-022-01074-w
citation: Wang et al., Nature Neuroscience 2022
code_url: https://git.fmrib.ox.ac.uk/falmagro/uk_biobank_pipeline_v_1.5
license: Apache-2.0
image: ghcr.io/astewartau/qsm-ci/matlab-ukb-qsm:v1
run: bash run.sh
matlab:
  entry: recon.m
  runtime: r2026a
params:
- name: t2star_ms
  default: 40
  description: assumed T2* in ms, setting the echo weights W_i = TE_i * exp(-TE_i / T2*)
- name: smvsize
  default: 12
  description: largest V-SHARP SMV kernel radius in mm
- name: padsize
  default: 64
  description: zero-pad size (voxels, isotropic) used by both the Laplacian unwrap and iLSQR
- name: mask_refine
  default: true
  description: apply the pipeline's own phase-variance mask trimming; set false to reconstruct
    strictly inside the mask the dataset supplies
